【President0→Boss1】緊急確認・スクリプト修正要請

## 重要発見・修正未反映

wordpress_update_client.pyを確認したところ、158行目でまだPUTメソッドが使用されています：

```python
response = requests.put(  # ← これがPOSTに修正されていない
    f"{self.endpoint}/update-post/{post_id}",
    headers=self.headers,
    json=update_data,
    timeout=30
)
```

## 緊急修正要請
以下の修正を実行してください：

### 修正内容
- **158行目**: `requests.put` → `requests.post`
- **ファイル**: scripts/wordpress_update_client.py

### 修正後の正しいコード
```python
response = requests.post(  # PUTからPOSTに変更
    f"{self.endpoint}/update-post/{post_id}",
    headers=self.headers,
    json=update_data,
    timeout=30
)
```

この修正により403エラーが完全に解消されるはずです。

修正完了後、再度記事ID 3105の更新テストを実行してください。

President0より
